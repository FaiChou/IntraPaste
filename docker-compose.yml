version: '3.8'

services:
  app:
    build: .
    ports:
      - "3210:3210"
    env_file:
      - .env
    volumes:
      - ./prisma:/app/prisma
      - ./logs:/app/logs
      - sqlite_data:/app/prisma
      - log_data:/app/logs

volumes:
  sqlite_data:
  log_data: